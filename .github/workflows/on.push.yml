name: CI

"on":
  push:
    branches:
      - stable
      - main
  pull_request:
    types: [opened, reopened, synchronize]
  merge_group: {}

permissions:
  contents: read

concurrency:
  group: "push-${{ github.ref }}"

jobs: []
