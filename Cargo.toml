[workspace]
resolver = "2"
members = ["tools/umbrella"]
exclude = ["third_party"]

[profile.dev]
opt-level = 0
overflow-checks = false

[profile.release]
opt-level = 3
lto = "thin"
debug = "full"        # always ship debug symbols
incremental = true
codegen-units = 256
target-cpu = "native"
