amends "elide:project.pkl"

name = "wsgi-flask"

python {
  wsgi {
    factory = "make_app"
    args { "Example" "1.0" }

    port = 3000
    workers = 1
  }
}

dependencies {
  pip {
      packages { "flask" }
  }
}
