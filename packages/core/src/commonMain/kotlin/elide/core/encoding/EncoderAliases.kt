package elide.core.encoding

import elide.core.encoding.base64.DefaultBase64
import elide.core.encoding.hex.DefaultHex

/** [DefaultHex] is now the default implementation. */
public typealias Hex = DefaultHex

/** [DefaultBase64] is now the default implementation. */
public typealias Base64 = DefaultBase64
