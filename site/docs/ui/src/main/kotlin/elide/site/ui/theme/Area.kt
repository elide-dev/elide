package elide.site.ui.theme

import web.cssom.ident

/** Area anchor definitions for the Elide site. */
object Area {
  /** Fixed app header area. */
  val Header = ident("header")

  /** Fixed app sidebar area. */
  val Sidebar = ident("sidebar")

  /** App content area. */
  val Content = ident("content")
}
