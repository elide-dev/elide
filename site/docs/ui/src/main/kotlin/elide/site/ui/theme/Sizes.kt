package elide.site.ui.theme

import csstype.px

/** [Area] size definitions for the Elide size. */
object Sizes {
  /** Size definitions for the Elide site header. */
  object Header {
    /** Header size. */
    val Height = 64.px
  }

  /** Size definitions for the Elide site sidebar. */
  object Sidebar {
    /** Sidebar width. */
    val Width = 275.px
  }
}
