package dev.elide.buildtools.bundler.cfg

/**
 * Represents a string assigned as an asset module ID.
 */
typealias AssetModuleId = String

/**
 * Full-length encoded fingerprint ("token") for an asset.
 */
typealias AssetToken = String

/**
 * Trimmed encoded fingerprint ("tag") for an asset.
 */
typealias AssetTag = String
