package dev.elide.buildtools.gradle.plugin.tasks

/** Versions of build tools depended on by the embedded JS build task. */
object Versions {
    const val esbuild = "0.15.14"
    const val prepack = "0.2.54"
}
